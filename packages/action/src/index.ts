/**
 * @anatrace/action — GitHub Action shell (placeholder).
 *
 * Foundation only: no Action logic (SARIF upload, sticky PR comment, exit codes)
 * — those land in Runbook 2. This stub exists to reserve the package slot and the
 * dependency edge onto the CLI.
 *
 * @returns A marker string identifying the placeholder.
 */
export function placeholder(): string {
  return '@anatrace/action: not implemented (Runbook 2)';
}
