node_modules
dist
coverage
.turbo
*.tsbuildinfo
.DS_Store

# P0.7 — local-only real-transcript corpus (true ground truth, scrubbed). NEVER committed:
# the repo is PUBLIC and scrub does not redact conversation/code. The committed
# fixtures/real/ corpus is real-FORMAT / synthetic-CONTENT; this is the deeper local-only check.
**/test/fixtures/real-local/
